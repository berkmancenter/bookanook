<%= render partial: 'greetings' %>

<% content_for :mail_message do %>
  This is a reminder for your upcoming reservation:
<% end %>

<%= render partial: 'reservation_info' %>

<% if @reservation.confirmed? %>
  <%= render partial: 'add_to_calendar' %>
<% end %>
