.admin {
  .left-menu {
    background-color: #ffffff;

    .filter {
      margin: 10px 0;

      .dropdown-menu {
        position: static;
        float: none;
        background-color: transparent;
        padding-left: 20px;
      }
    }
  }
}